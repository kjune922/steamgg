# AGENTS.md

## 목적
이 저장소는 게임 탐색 웹 MVP를 구현하기 위한 모노레포다.

현재 1차 MVP 범위는 아래 3개 페이지를 중심으로 한다.
- ROOT: 검색창, 인기 게임 일부 노출, 로그인 버튼 UI
- LIST: 검색 결과 목록, 필터, 정렬
- DETAIL: 게임 설명, 구매 링크, 유사 게임

이 문서는 저장소 전체에 적용되는 공통 작업 규칙을 정의한다.
더 깊은 경로에 있는 AGENTS.md가 해당 디렉토리에서는 우선 적용된다.

## 저장소 구조
- frontend: Next.js + TypeScript 프론트엔드
- backend: Spring Boot 백엔드
- infra: 배포 및 인프라 관련 설정
- docs: 작업 문맥, 구조 요약, 실행 계획, 인계 메모

## 공통 작업 원칙
- 작업 전에 루트 AGENTS.md와 현재 작업 대상 디렉토리의 AGENTS.md를 먼저 읽는다.
- 바로 수정하지 말고 관련 파일과 현재 구조를 먼저 파악한다.
- 여러 영역을 동시에 수정할 경우, 왜 함께 수정해야 하는지 먼저 정리한다.
- 큰 작업은 바로 확장하지 말고 필요한 경우 docs/exec-plans/active/에 계획을 남긴다.
- 요청 범위를 넘는 리팩토링은 하지 않는다.
- 운영 환경 설정, 배포 설정, 비밀값은 임의로 바꾸지 않는다.
- 비밀값, 토큰, 키, 실제 운영 주소는 코드, 커밋, 로그에 남기지 않는다.
- 절대경로를 하드코딩하지 않는다.
- 작업 종료 전 docs/continuity/current.md를 갱신한다.

## 코드 작성 공통 규칙
- 한 작업에서 프론트엔드, 백엔드, 인프라를 동시에 크게 수정하지 않는다.
- 중복 코드를 만들기 전에 기존 구조와 패턴을 먼저 확인한다.
- 기존 naming convention과 파일 스타일을 우선 따른다.
- 디버그용 로그, 임시 코드, 주석 처리된 불필요한 코드는 남기지 않는다.
- 하드코딩한 경로, URL, 비밀값, 환경별 값은 넣지 않는다.
- 기능 변경과 구조 정리는 가능하면 분리한다.
- 구현은 현재 MVP 목표와 직접 연결되는 범위 안에서만 확장한다.

## 파일 수정 원칙
- frontend, backend, infra 각각의 세부 규칙은 해당 디렉토리의 AGENTS.md를 따른다.
- 스키마, 환경 설정, 배포 관련 변경은 영향 범위를 먼저 확인한다.
- 작업 범위와 직접 관련 없는 파일은 수정하지 않는다.
- 자동 생성 파일이나 빌드 결과물은 특별한 이유가 없으면 커밋하지 않는다.

## 문서 작성 규칙
- docs/code-map: 구조 요약, 파일 흐름, 호출 관계 정리
- docs/context-packs: 특정 작업에 필요한 문맥, 실패 원인, 관련 파일 정리
- docs/exec-plans: 비교적 큰 작업의 실행 계획
- docs/continuity/current.md: 다음 작업자가 바로 이어받을 수 있는 인계 메모

문서는 길게 늘어놓기보다, 다음 작업자가 바로 이해할 수 있게 짧고 명확하게 쓴다.

## 커밋 전 검증 규칙
- 수정한 영역의 빌드, 테스트, lint 또는 typecheck를 먼저 실행한다.
- 실행하지 못한 검증이 있으면 이유를 남긴다.
- 변경 사항이 현재 작업 목표와 직접 관련 있는지 다시 확인한다.
- 민감정보, 토큰, 비밀값, 실제 운영 주소가 커밋에 포함되지 않았는지 확인한다.
- 불필요한 콘솔 로그와 임시 파일이 남아 있지 않은지 확인한다.
- 문서가 필요한 변경이면 docs와 continuity 메모를 갱신한다.

## 커밋 메시지 규칙
- 커밋 메시지는 prefix와 핵심 변경 요약으로 시작한다.
- prefix는 아래 중 하나를 사용한다.
    - feat:
    - fix:
    - refactor:
    - docs:
    - chore:
    - test:
- 제목은 짧고 명확하게 어떤 변경인지 드러나야 한다.
- 하나의 커밋에는 가능하면 하나의 주제만 담는다.

### 권장 형식
prefix: 핵심 변경 요약

무엇을 수정했는지 1문장
왜 이 변경이 필요한지 1문장

### 예시
feat: LIST 페이지에 장르 필터와 인기순 정렬을 추가

검색 결과에서 사용자가 조건을 다시 좁혀가며 게임을 탐색할 수 있도록 장르 필터와 인기순 정렬 UI를 연결했다.
MVP의 핵심 흐름인 검색 → 탐색 → 상세 진입을 더 자연스럽게 만들기 위한 변경이다.

### 커밋 메시지 작성 원칙
- 가능하면 커밋 본문에 1~2문장으로 아래를 함께 남긴다.
    - 무엇을 수정했는지
    - 왜 이 변경이 필요한지
- 본문은 단순 작업 나열보다 아래 관점이 드러나게 작성한다.
    - 사용자 흐름 개선
    - 구조 단순화
    - 유지보수성 향상
    - MVP 범위 명확화
    - 확장성 고려
- 나중에 Git 로그만 봐도 프로젝트 기여와 판단 근거가 보이도록 작성한다.
- 커밋 메시지는 포트폴리오와 면접에서 설명 자료로 재사용될 수 있도록 쓴다.

## 브랜치 및 푸시 규칙
- main 브랜치에 직접 작업하거나 직접 push하지 않는다.
- feature/*, fix/* 같은 작업 브랜치를 사용한다.
- 큰 변경은 push 전에 변경 목적과 범위를 짧게 정리한다.
- push 전에 최신 변경사항을 pull 또는 rebase로 반영하고 충돌 여부를 확인한다.
- 커밋 또는 push를 준비할 때는 반드시 검증과 체크리스트를 먼저 확인한다.

## 푸시 전 체크리스트
- 현재 변경이 요청 범위를 넘지 않는가
- 관련 빌드와 검증이 끝났는가
- 불필요한 파일이 포함되지 않았는가
- 민감정보가 포함되지 않았는가
- 필요한 문서가 갱신되었는가
- docs/continuity/current.md에 현재 상태를 남겼는가

## 완료 전 체크
- 수정 범위가 요청 범위를 넘지 않았는지 확인한다.
- 관련 빌드나 테스트 명령을 실행한다.
- 남은 이슈나 미완료 사항이 있으면 docs/continuity/current.md에 적는다.
- 다음 작업자가 이어서 볼 파일과 주의할 점을 남긴다.
- 작업 전에 docs/mvp/mvp-v1.md를 읽고 현재 MVP 범위를 확인한다.
- 요청이 없으면 MVP 범위를 넘는 기능을 추가하지 않는다